import React, {Component} from 'react';

class cart extends Component {
  state = {};
  render () {
    return (
      <React.Fragment>
        {'cart'}
      </React.Fragment>
    );
  }
}

export default cart;
